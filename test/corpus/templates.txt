==================
Bare template
==================

{{abc}}
---

(fragment
  (template
    (templatename)))
  
==================
Template with unnamed arg
==================

{{abc|1}}
---

(fragment
  (template
     (templatename)
     (templateparam
       (templatevalue))))

==================
Template with named arg
==================

{{abc|key=value}}
---

(fragment
  (template
     (templatename)
     (templateparam
        (templatekey)
        (templatevalue))))

==================
Recursive template
==================

{{abc| {{de}} }}
---

(fragment
  (template
     (templatename)
     (templateparam
        (templatevalue
           (template
              (templatename))))))

==================
Parser function
==================

{{#ifeq:{{#time:Y}}|2022|true}}
---

(fragment
  (template
     (templatename)
     (template
         (templatename))
     (templateparam
        (templatevalue))
     (templateparam
        (templatevalue))))
